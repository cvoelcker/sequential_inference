# @package experiment.model_algorithm

defaults:
  - encoder: image_encoder
  - decoder: broadcast_decoder

_target_: sequential_inference.algorithms.env_models.dreamer.DreamerModelAlgorithm

action_dim: ${env.action_dim}
kl_factor: 1.
state_factor: 1.
reward_factor: 1.
feature_dim: 64
latent_dim: 64
recurrent_hidden_dim: 64 
belief_dim: 64
hidden_units: [64, 64]
leaky_slope: 0.2
reward_decoder_hidden_units: [64, 64]
predict_from_prior: False
condition_on_posterior: False