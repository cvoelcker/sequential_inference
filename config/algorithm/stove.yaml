# @package _group_

name: SimpleStove

parameters:
  feature_dim: 64
  latent_dim: 32
  dynamics_latent_dim: 32
  hidden_units:
    - 64
    - 64
  leaky_slope: 0.2
  predict_from_prior: False
  condition_on_posterior: False
  kl_factor: 1.
  state_factor: 1.
  reward_factor: 1.
